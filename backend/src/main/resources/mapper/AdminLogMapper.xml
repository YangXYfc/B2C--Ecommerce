<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.team.ecommerce.admin.mapper.AdminLogMapper">

    <resultMap id="logMap" type="com.team.ecommerce.admin.entity.AdminLogEntity">
        <constructor>
            <idArg column="id" javaType="Long"/>
            <arg column="admin_id" javaType="Long"/>
            <arg column="action" javaType="String"/>
            <arg column="target_type" javaType="String"/>
            <arg column="target_id" javaType="Long"/>
            <arg column="detail" javaType="String"/>
            <arg column="ip_address" javaType="String"/>
            <arg column="created_at" javaType="java.time.LocalDateTime"/>
        </constructor>
    </resultMap>

    <select id="selectById" resultMap="logMap">
        SELECT * FROM admin_log WHERE id = #{id}
    </select>

    <select id="selectPage" resultMap="logMap">
        SELECT * FROM admin_log
        <where>
            <if test="action != null and action != ''">AND action = #{action}</if>
        </where>
        ORDER BY created_at DESC
        LIMIT #{offset}, #{size}
    </select>

    <select id="countByAction" resultType="long">
        SELECT COUNT(*) FROM admin_log
        <where>
            <if test="action != null and action != ''">AND action = #{action}</if>
        </where>
    </select>

    <insert id="insert">
        INSERT INTO admin_log (admin_id, action, target_type, target_id, detail, ip_address)
        VALUES (#{adminId}, #{action}, #{targetType}, #{targetId}, #{detail}, #{ipAddress})
    </insert>

</mapper>
