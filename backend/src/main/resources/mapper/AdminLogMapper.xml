<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.team.ecommerce.admin.mapper.AdminLogMapper">

    <!-- created_at 走数据库默认 CURRENT_TIMESTAMP -->
    <insert id="insert" parameterType="com.team.ecommerce.admin.entity.AdminLog"
            useGeneratedKeys="true" keyProperty="id">
        INSERT INTO admin_log (admin_id, action, target_type, target_id, detail, ip_address)
        VALUES (#{adminId}, #{action}, #{targetType}, #{targetId}, #{detail}, #{ipAddress})
    </insert>

</mapper>
