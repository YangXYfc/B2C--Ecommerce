<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.team.ecommerce.trade.order.mapper.ProductReadMapper">

    <select id="selectSkuInfo" resultType="com.team.ecommerce.trade.order.mapper.ProductReadMapper$SkuInfo">
        SELECT
            sku.id           AS skuId,
            sku.sku_name     AS skuName,
            sku.price        AS price,
            p.id             AS productId,
            p.name           AS productName,
            p.main_image     AS productImage,
            p.merchant_id    AS merchantId
        FROM product_sku sku
        JOIN product p ON sku.product_id = p.id
        WHERE sku.id = #{skuId}
    </select>

</mapper>
