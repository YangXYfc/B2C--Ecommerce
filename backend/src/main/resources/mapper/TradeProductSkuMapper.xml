<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.team.ecommerce.trade.inventory.TradeProductSkuMapper">
    <select id="selectStockForUpdate" resultType="int">
        SELECT stock FROM product_sku WHERE id = #{skuId} FOR UPDATE
    </select>
    <update id="deductStock">
        UPDATE product_sku SET stock = stock - #{quantity}
        WHERE id = #{skuId} AND stock >= #{quantity}
    </update>
    <update id="restoreStock">
        UPDATE product_sku SET stock = stock + #{quantity}
        WHERE id = #{skuId}
    </update>
</mapper>
